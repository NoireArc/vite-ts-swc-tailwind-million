class Schedule {
  constructor(id, title, time, classroomId, classroom, createdAt, updatedAt) {
    this.id = id;
    this.title = title;
    this.time = time;
    this.classroomId = classroomId;
    this.classroom = classroom;
    this.createdAt = createdAt;
    this.updateAt = updatedAt;
  }
}

export default Schedule;
